= link_to t('shared.add_new_item'), new_payment_path, class: 'btn btn-default'
%table.table.table-condensed
  %thead
    %tr
      %th= t('payments.columns.student')
      %th= t('payments.columns.for_month')
      %th= t('shared.actions')
  %tbody
    = render payments
